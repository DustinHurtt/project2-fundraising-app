<h2>My Campaigns</h2>

<div class=" ">
    {{#each myCampaigns}}

    <ul>

  
        <img src="{{this.imageUrl}}" alt="room pic">  
        <p> {{this.name}} </p>
        <p> {{this.description}} </p>
        <p> This is a {{this.duration}} week campaign.</p>
        <p> Goal: {{this.readableGoal}} </p>
        <p> We have received {{this.donations}} donations so far.</p>
        <p> {{this.readableTotal}} has been donated. </p>
        <p> We are {{this.percent}}% to goal. </p>
        <p> This campaign ends on {{this.deadline}} </p>
        <p> There are {{this.timeLeft.days}} days, {{this.timeLeft.hours}} hours, and {{this.timeLeft.minutes}} minutes before this campaign ends. </p>
        <ul>
        {{#each this.pledges}}
        <li>
            <h5>{{this.user.fullName}} donated ${{this.amount}}</h5>
            <p>-"{{this.comment}}"</p>
        </li>
        {{/each}}
        </ul>
        <br>

        <a href="/campaigns/{{this._id}}/edit">Update Campaign</a>

        <form method = "post" action="/campaigns/{{this._id}}/delete">
            <input type="submit" value="Delete Campaign" />
        </form>

<hr>

</ul>


    {{/each}}

</div>